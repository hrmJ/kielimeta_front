import React from 'react';
import styles from './footer_styles.scss';

export default () => (
  <footer className={styles.sitefooter}>
    <div className={styles.footerContent}>lisätietoa ym.</div>
  </footer>
);
